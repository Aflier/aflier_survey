<%= form_with(model: question_input, local: true) do |form| %>
  <% if question_input.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_input.errors.count, "error") %> prohibited this question_input from being saved:</h2>

      <ul>
      <% question_input.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :calc_id %>
    <%= form.number_field :calc_id %>
  </div>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.number_field :question_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
