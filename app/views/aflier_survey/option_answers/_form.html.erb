<%= form_with(model: option_answer, local: true) do |form| %>
  <% if option_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(option_answer.errors.count, "error") %> prohibited this option_answer from being saved:</h2>

      <ul>
      <% option_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :answer_id %>
    <%= form.number_field :answer_id %>
  </div>

  <div class="field">
    <%= form.label :option_id %>
    <%= form.number_field :option_id %>
  </div>

  <div class="field">
    <%= form.label :unique_ident %>
    <%= form.number_field :unique_ident %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
