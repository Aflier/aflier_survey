<%= simple_form_for(@question_section) do |f| %>
  <%= render 'aflier_survey/bootstrap/modal',
             btn_title: "New Question section", f: f do %>
    <%= f.input :name %>
    <%= f.input :description %>
  <% end %>
<% end %>