<%= form_with(model: calc_input, local: true) do |form| %>
  <% if calc_input.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calc_input.errors.count, "error") %> prohibited this calc_input from being saved:</h2>

      <ul>
      <% calc_input.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :calc_id %>
    <%= form.number_field :calc_id %>
  </div>

  <div class="field">
    <%= form.label :calc_input_id %>
    <%= form.number_field :calc_input_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
